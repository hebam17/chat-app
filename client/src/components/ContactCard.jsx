import React from "react";

export default function ContactCard({
  userId,
  online,
  username,
  createGroup,
  handleAdding,
  AddingToGroup,
  removingFromGroup,
}) {
  return <div>ContactCard</div>;
}
